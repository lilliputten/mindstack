# Cloned content of the `multilingual-stemmer` library

@see https://www.npmjs.com/package/multilingual-stemmer

It's copied from the node module:

```
node_modules\.pnpm\multilingual-stemmer@1.0.2\node_modules\multilingual-stemmer\dist
```
