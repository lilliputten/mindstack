import { generateMetadata, generateStaticParams, RootLayout, viewport } from './RootLayout';

export const dynamic = 'force-static';

export { generateMetadata, generateStaticParams, viewport };
export default RootLayout;
