export default function NoSkeletonForModalSlots() {
  return null;
  /* // No skeletons for modal slots!
   * return (
   *   <GenericSkeleton
   *     className={cn(
   *       isDev && '__ManageCategoriesListSkeleton_suggest', // DEBUG
   *     )}
   *   />
   * );
   */
}
